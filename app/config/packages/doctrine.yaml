doctrine:
    dbal:
        url: '%env(resolve:DB_URL)%'
    orm:
        auto_generate_proxy_classes: true
        naming_strategy: doctrine.orm.naming_strategy.underscore_number_aware
        auto_mapping: true
        mappings:
            Employee:
                is_bundle: false
                type: annotation
                dir: '%kernel.project_dir%/src/Module/Employee/Model'
                prefix: 'Employee\Model'
                alias: Employee
            Department:
                is_bundle: false
                type: annotation
                dir: '%kernel.project_dir%/src/Module/Department/Model'
                prefix: 'Department\Model'
                alias: Department
            Grade:
                is_bundle: false
                type: annotation
                dir: '%kernel.project_dir%/src/Module/Grade/Model'
                prefix: 'Grade\Model'
                alias: Grade